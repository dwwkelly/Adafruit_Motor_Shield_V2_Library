CMAKE_MINIMUM_REQUIRED(VERSION 2.8.11)
PROJECT(Adafruit_Motor_Shield)

SET(CMAKE_CXX_FLAGS "-std=c++11 -Wall")

SET(INCLUDE_DIR include)

INCLUDE_DIRECTORIES(${INCLUDE_DIR})

ADD_SUBDIRECTORY(lib)
ADD_SUBDIRECTORY(examples)
